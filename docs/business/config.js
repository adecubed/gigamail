// Configure a same-origin POST endpoint returning {"ok":true}, or a verified
// demo inbox. No data leaves the browser until the visitor submits this form.
export const demoConfig = { endpoint: '', email: '' };
